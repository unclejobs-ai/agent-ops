# Claude Code changelog check

- Checked at: 2026-03-24T14:00:42.587Z
- Installed version: 2.1.76
- Latest changelog version: 2.1.81
- Previously seen version: 2.1.81
- New update found: no
- Installed behind latest: yes

## Install status

- 로컬 Claude Code는 2.1.76
- 최신 changelog 기준 버전은 2.1.81
- 즉, 지금 설치본이 최신보다 뒤처져 있다.

## Latest version snapshot (2.1.81)

- Added `--bare` flag for scripted `-p` calls — skips hooks, LSP, plugin sync, and skill directory walks; requires `ANTHROPIC_API_KEY` or an `apiKeyHelper` via `--settings` (OAuth and keychain auth disabled); auto-memory…
- Added `--channels` permission relay — channel servers that declare the permission capability can forward tool approval prompts to your phone
- Fixed multiple concurrent Claude Code sessions requiring repeated re-authentication when one session refreshes its OAuth token
- Fixed voice mode silently swallowing retry failures and showing a misleading "check your network" message instead of the actual error
- Fixed voice mode audio not recovering when the server silently drops the WebSocket connection

## Telegram-ready summary

Claude Code 설치본이 최신보다 뒤쳐져 있다: 현재 2.1.76 / 최신 2.1.81
- 설치 버전: 2.1.76
- 최신 버전: 2.1.81
- Added `--bare` flag for scripted `-p` calls — skips hooks, LSP, plugin sync, and skill directory walks; requires `ANTHROPIC_API_KEY` or an `apiKeyHelper` via `--settings` (OAuth and keychain auth disabled); auto-memory…
- Added `--channels` permission relay — channel servers that declare the permission capability can forward tool approval prompts to your phone
- Fixed multiple concurrent Claude Code sessions requiring repeated re-authentication when one session refreshes its OAuth token
- Fixed voice mode silently swallowing retry failures and showing a misleading "check your network" message instead of the actual error
- Fixed voice mode audio not recovering when the server silently drops the WebSocket connection

원문: https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md
